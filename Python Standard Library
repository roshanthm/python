~~Working with Arrays
import array
arr = array.array('i', [1, 2, 3, 4])
print(arr)                                         //array('i', [1, 2, 3, 4])

~~Mathematical Operations
import math
print(math.sqrt(16))
print(math.pi)

~~Generating Random Numbers
import random
print(random.randint(1, 10))
fruits = ['apple', 'banana', 'cherry']
print(random.choice(fruits))

~~File and Directory Operations with OS Module
import os
print(os.getcwd())
os.mkdir('test_dir')                                   //You can get the current working directory and create new directories using this module.

~~High-Level File Operations with shutil
import shutil
shutil.copyfile('source.txt', 'destination.txt')



~~Date and Time Operations
from datetime import datetime, timedelta
now = datetime.now()
print(now)
yesterday = now - timedelta(days=1)
print(yesterday)


~~Program Timing with Time Module
import time
print(time.time())
time.sleep(2)
print(time.time())


~~~Regular Expressions with re Module
import re
pattern = r'\d+'
text = 'There are 123 apples.'                           The re module is used for regular expressions, which help in matching specific patterns in text. 
                                                         For example, you can search for digits in a string using a pattern.
match = re.search(pattern, text)
if match:
    print(match.group())
